<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.github.vmarquet.bartendr.MainActivity" >

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="@string/hello_world"
        android:textSize="32sp"
        android:layout_marginTop="20dp" />

    <Button
        android:id="@+id/order_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="@string/order_button"
        android:onClick="seeMenu" />

    <Button
        android:id="@+id/pay_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/order_button"
        android:layout_alignStart="@+id/order_button"
        android:layout_alignRight="@+id/order_button"
        android:layout_alignEnd="@+id/order_button"
        android:layout_below="@+id/order_button"
        android:text="@string/pay_button"
        android:onClick="pay" />
    <!-- Nota bene: alignLeft and alignRight are the older equivalents of alignStart and alignEnd -->
    <!-- they are left here only for compatibility with older devices -->

</RelativeLayout>